define(['jquery', 'domReady', 'modules/Prayer'], function($, domReady, Prayer) {
    var page = {
        init: function init() {
            domReady(function() {

            });
        }
    };

    page.init();
});
